var path=require('path');
var fs = require('fs');
var lib = path.join(path.dirname(fs.realpathSync(__filename)), '../lib');

var simple = require(lib + '/calc_simple.js');
var advanced = require(lib+ '/calc_avanzado.js');
module.exports={
	sumar : simple.sumar,
	restar: simple.restar,
	multiplicar: advanced.multiplicar,
	dividir: advanced.dividir
}